import { reminderOption } from "../common/interface";

export const reminderService={
    add:async (data:reminderOption):Promise<void>=>{

    },
    off:async (data:reminderOption):Promise<void>=>{

    },
    repair: async (): Promise<void>=>{
        
    }
}