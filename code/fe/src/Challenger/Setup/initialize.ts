/**
 * Module không có dữ liệu dựng sẵn: thử thách là thứ người dùng tự đặt, nên nạp
 * một bộ gợi ý vào máy sẽ là app tự đặt mục tiêu thay họ. `AppSetup/initialize`
 * gọi hàm này cho mọi module nên nó vẫn phải tồn tại.
 */
export async function initialize() {}
