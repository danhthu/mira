# Bảng câu hỏi — V2

> Sinh bởi skill `phan-tich-yeu-cau`, 2026-08-25. Xếp theo mức chặn, nặng lên trên. Mã tiếp số từ Q-009 (Q-001 đến Q-008 đã dùng ở V1, kể cả khoảng trống Q-007 không dùng — giữ nguyên, không lấp lại). Đọc lướt thấy phương án đề xuất ổn thì trả lời "ok hết" là G1 coi như xong; câu nào không trả lời sẽ tự chuyển thành giả định trong `gia-dinh-v2.md`.

## Mức Chặn

| Mã | Vấn đề | Phương án đề xuất | Hệ quả nếu chọn sai | Ai chốt |
|---|---|---|---|---|
| Q-009 | Đọc SMS ngân hàng (R-087) chỉ khả thi trên Android — iOS không cấp API công khai cho app bên thứ ba đọc nội dung SMS đến. Mâu thuẫn trực tiếp với ràng buộc cứng "chạy được trên cả iOS và Android" (R-051, áp dụng toàn bộ vòng đời sản phẩm). | Android: đọc SMS trực tiếp, xin quyền hệ điều hành. iOS: thay bằng luồng nhập tay có trợ giúp — dán nội dung SMS (share sheet từ app Tin nhắn), hoặc chụp ảnh SMS rồi OCR số tiền. Cả hai nền tảng đều tạo `expense` nháp `confirmed = false`, người dùng xác nhận 1 chạm giống nhau. | Nếu bỏ qua khác biệt nền tảng, tính năng chạy được trên Android nhưng vô dụng trên iOS — vi phạm ràng buộc chạy đa nền tảng mà không ai phát hiện tới lúc build. | Người dùng |
| Q-010 | `code/fe/src/core/lifeRate.ts` tính `lifeRate = netIncome / realWorkHours` trên dữ liệu đã coi là tổng theo tháng, không nhân hệ số `× 52/12` như công thức viết trong `docs/03-formulas.md` dòng 27. Xem phân tích đầy đủ ở mục "Đối chiếu code hiện có" trong `yeu-cau-v2.md`. | Coi cách hiểu của code là đúng: `WorkPeriodInput` nhận dữ liệu đã gộp từ nhiều bản ghi `workLoad` theo tuần thành tổng thật của một tháng — không cần ước lượng bằng hệ số `52/12` nữa vì đã có số thật. Cập nhật `docs/03-formulas.md` để ghi rõ hai trường hợp: có dữ liệu tuần thật (cộng dồn trực tiếp) và chỉ có một con số đại diện/tuần (nhân `52/12` để ước lượng). | Nếu code sai và công thức gốc đúng, mọi tỷ giá đời đã hiển thị lệch khoảng 4,33 lần — số liền sau quyết định cả Vốn tự do lẫn Quy đổi chi tiêu vì cả hai đều dùng `lifeRate`. | Người dùng |
| Q-011 | Ví chung (R-089) cần cấu trúc dữ liệu nào để một khoản chi hoặc một khoản tiền thuộc về "các bên" thay vì một mình Người dùng — hiện `expense`, `money` không có cột liên kết tới `person`. `space` (bảng cho không gian chung thật sự multi-user) chỉ bật ở V3. | Thêm một cột nhẹ trên `expense` (ví dụ `sharedWithPersonId?`) thay vì tạo bảng mới ở V2, giữ đúng tinh thần R-053 (không thiết kế lại schema giữa chừng) bằng cách coi đây là một cột optional bổ sung chứ không phải bảng mới. Khi tới V3, `space` thay thế hoàn toàn cơ chế tạm này. | Nếu không có cột liên kết, Ví chung không phân biệt được khoản chi của ai trong ví — tính năng chỉ còn là ghi chú tên gợi nhớ, không "minh bạch" như mô tả nguồn. | Người dùng |
| Q-012 | Ví chung có cần đồng bộ dữ liệu giữa hai thiết bị của hai người thật hay không, hay chỉ là Người dùng tự ghi chép rồi chia sẻ thủ công (ảnh chụp màn hình, xuất JSON)? Xem phân tích ba khả năng ở `phan-quyen-v2.md`. | Chọn khả năng 1 (ghi chép một chiều, không đồng bộ hai máy) cho V2 — giữ nguyên kiến trúc local-first của R-011, để việc multi-user thật đúng nghĩa dồn hết vào M7 (V3) như roadmap đã định. | Nếu người dùng thật sự cần hai máy cùng nhìn một Ví chung ngay ở V2, phương án 1 sẽ khiến tính năng "vô dụng trong trạng thái rỗng" — đúng cảnh báo mà `04-roadmap.md` dòng 5 đã nói trước cho trường hợp bật sai thứ tự phụ thuộc. | Người dùng |
| Q-018 | Hai tính năng của M6 — "nhiệt kế quan hệ" và "nhắc ngày quan trọng" — không có bảng hay trường nào trong 13 bảng đã chốt ở `02-data-model.md` để lưu. R-053 (V1, vẫn hiệu lực) cấm "thiết kế lại schema giữa chừng". "Nhật ký gặp gỡ 1 dòng" có thể tạm dùng `moment` sẵn có, nhưng cũng chưa được xác nhận là đúng ý đồ ban đầu. | Coi đây là hai trường hợp cần bổ sung cột (không phải bảng mới, để giữ đúng tinh thần R-053): thêm `relationshipScore?: number` và `importantDates?: json` vào `person`. "Nhật ký gặp gỡ" tái dùng `moment` với `personIds` chỉ có đúng một người. | Nếu R-053 phải hiểu chặt là "không thêm cả cột mới", hai tính năng này không triển khai được ở V2 mà không vi phạm ràng buộc đã chốt — cần người dùng quyết định mức độ chặt của R-053 trước khi code chạm tới `db/schema.ts`. | Người dùng |

## Mức Rủi ro

| Mã | Vấn đề | Phương án đề xuất | Hệ quả nếu chọn sai | Ai chốt |
|---|---|---|---|---|
| Q-013 | Gợi ý hẹn gặp (R-098) cần "lịch trống", nhưng Đồng bộ lịch (Google/Apple Calendar) đã được người dùng xác nhận hoãn ra sau V1 (Q-003 gốc). Roadmap V2 chỉ nói phụ thuộc `person` và `timeEntry`, không nhắc gì tới lịch. | Suy "lịch trống" từ dữ liệu đã có: khung giờ chưa bị `timeEntry` bucket `work` chiếm trong tuần, cộng ngân sách giờ khoang `people` còn dư so với `desiredCadence`. Không đọc lịch thật ở V2. | Nếu người dùng kỳ vọng gợi ý dựa trên lịch thật, kết quả sẽ gợi ý sai giờ đã có việc khác không nằm trong `timeEntry` — cần nói rõ giới hạn này trong UI, không phải lỗi kỹ thuật. | Người dùng |
| Q-014 | Ngân sách 6 khoang giá trị áp cho tiền (R-086, UC-24) chưa có công thức đối chiếu vượt/còn dư — `03-formulas.md` mục 6 chỉ viết công thức cho giờ (168 giờ/tuần), không có bản tương ứng cho tiền. | Dùng cùng cấu trúc `allocated / unallocated / borrowed` như mục 6, đổi đơn vị từ phút sang VND và mốc từ 168 giờ/tuần sang `monthlyExpense` hoặc thu nhập tháng. | Nếu công thức thật khác (ví dụ theo % thu nhập thay vì số tiền tuyệt đối), phải làm lại toàn bộ màn hình ngân sách tiền. | Người dùng |
| Q-015 | Chi tiêu vô nghĩa (R-090) cần đối chiếu `expense` với `moment` trong một cửa sổ thời gian, nhưng cửa sổ đó (cùng ngày, cùng tuần) chưa nguồn nào định nghĩa. | Cùng ngày (`expense.occurredAt` và `moment.occurredAt` trùng ngày dương lịch), không yêu cầu `personIds` giao nhau — vì bản thân định nghĩa "vô nghĩa" chỉ nói "không để lại khoảnh khắc", không nói "không để lại khoảnh khắc liên quan tới ai". | Nếu cửa sổ đúng phải rộng hơn (cùng tuần), danh sách sẽ liệt oan nhiều khoản chi có khoảnh khắc ghi muộn vài ngày sau. | Người dùng |
| Q-017 | Nhiệt kế quan hệ (R-096, UC-28) — không nguồn nào mô tả cách tính: tự động suy từ tần suất gặp so với `desiredCadence`, hay Người dùng tự đánh giá cảm nhận bằng tay. | Người dùng tự đánh giá bằng tay (thang 1-5, giống `mood.level` đã có trong schema cho V5), không suy diễn tự động ở V2 — đơn giản hơn và không cần thêm logic tính toán mới. | Nếu đúng ra phải tự động suy từ dữ liệu gặp gỡ, để người dùng tự nhập sẽ biến "nhiệt kế" thành một dạng đánh giá chủ quan, khác hẳn tinh thần "sổ cái" khách quan của Mira. | Người dùng |

## Mức Để sau

| Mã | Vấn đề | Phương án đề xuất | Hệ quả nếu chọn sai | Ai chốt |
|---|---|---|---|---|
| Q-016 | Mô phỏng đường tự do (R-091, UC-25) — "giảm 1 ngày làm/tuần cần bao lâu tiết kiệm" — không có công thức nào trong `03-formulas.md`, chỉ có tên tính năng ở `01-modules.md`. | Suy tạm từ các công thức đã có: giảm 1 ngày làm/tuần làm giảm thu nhập ước tính = `lifeRate × (realWorkHours/tuần / 5 ngày làm)`; số tháng cần tiết kiệm thêm để bù = mức giảm thu nhập chia `monthlySaving` hiện tại. Cần xác nhận công thức chính thức khi bắt đầu code UC-25, không cần chốt ngay ở vòng G1. | Nếu công thức chính thức khác, chỉ cần sửa lại UC-25 và phần tính toán liên quan, không ảnh hưởng các UC khác. | Người dùng — có thể hỏi lại khi bắt đầu code UC-25 |

---

## Trả lời

*(để trống, chờ người dùng — không tự trả lời câu mức Chặn theo đúng luật gốc của skill)*
